# Search

Search for songs by keyword. Returns track ID, title, duration, and more.

```bash
vncmd search <keyword> [-n count] [--offset offset]
```

| Flag | Short | Description |
| --- | --- | --- |
| `--limit N` | `-n` | Max results (default: 30) |
| `--offset N` | `/` | Pagination offset |
